<Application x:Class="ShutdownManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             xmlns:local="clr-namespace:ShutdownManager.ViewModels"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>

        <ContextMenu x:Shared="false" x:Key="SysTrayMenu">
            <MenuItem Header="Show Window" Command="{Binding ShowWindowCommand}"  />
            <MenuItem Header="Hide Window" Command="{Binding HideWindowCommand}"/>
            <Separator />
            <MenuItem Header="Startup" Command="{Binding AddToStartup}" IsCheckable="True" IsChecked="{Binding IsAutoStartChecked, Mode=OneWay}"/>
            <Separator/>
            <MenuItem Header="Exit" Command="{Binding ExitApplicationCommand}">
                <MenuItem.Icon>
                    <Image Width="16"
                           Height="16"
                           Source="/images/Close.png" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>



        <tb:TaskbarIcon x:Key="Taskbar" VerticalAlignment="Top" ToolTipText="ShutdownManager"
                                    IconSource="/icons/ShutdownManager.ico"                                   
                                    DoubleClickCommand="{Binding ShowWindowCommand}"
                                    ContextMenu="{StaticResource SysTrayMenu}">     


            <tb:TaskbarIcon.DataContext>
                    <local:NotifyIconViewModel/>
            </tb:TaskbarIcon.DataContext>
        </tb:TaskbarIcon>


    </Application.Resources>
</Application>
